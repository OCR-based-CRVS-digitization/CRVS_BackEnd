{
  "scripts": {
    "start": "node ./index",
    "dev": "nodemon ./index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npx prisma generate && node ./index"
  },
  "dependencies": {
    "@prisma/client": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "latest",
    "express-fileupload": "^1.4.0",
    "firebase": "^10.1.0",
    "jsonwebtoken": "latest",
    "multer": "^1.4.5-lts.1",
    "pg": "latest",
    "prompt": "latest",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.9"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.1.1"
  },
  "engines": {
    "node": ">=17.0.0"
  }
}
